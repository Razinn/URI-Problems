#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    double a,b,r;
    cin >> a >> b;
    r = a * b;
    cout << "PROD = " << r << "\n";

    return 0;
}

